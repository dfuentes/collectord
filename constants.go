package main

// file paths

const (
	CONFIG_PATH string = "/etc/collectord/conf.json"
)

// env variables

const (
	CONFIG_ENV string = "COLLECTORD_CONFIG_PATH"
)

// network constants

const (
	MIN_PACKET_THRESHOLD = 4096
)
